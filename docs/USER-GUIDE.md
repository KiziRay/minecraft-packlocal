# 模組包翻譯工具使用說明

給一般 Minecraft 玩家使用的繁體中文工具。你不需要懂 jar、JSON 或資源包格式，照著畫面操作即可。

## 快速開始

1. 在「翻譯整合包」頁面選取你實際遊玩的整合包資料夾。
2. 選取翻譯輸出位置。工具會建立獨立的 `翻譯結果/`，不會直接改寫原本整合包。
3. 想補上沒有繁中的英文時，勾選 AI 並確認金鑰；只做簡繁轉換時可以關閉 AI。
4. 按「開始翻譯」，等待進度完成。
5. 先用「開啟輸出」查看結果，確認後再按「套用到遊戲」。

## 介面與主題

工具預設使用深色模式，適合長時間查看翻譯進度與日誌。按右上角的「深色／淺色」即可切換；選擇會保存在這台電腦，下次開啟會沿用，不會同步到其他裝置。

## 翻譯整合包服務

這項服務會讀取模組語言檔，處理以下內容：

- 已有簡體中文的文字：轉成台灣繁體中文與常用台灣用語。
- 沒有繁中的英文文字：可使用 AI 補翻。
- 語言檔中的格式佔位符：會保護 `%s`、`%1$s`、`{0}`、`§` 等內容，不符合時退回原文。
- 資源包、FTB Quests、Patchouli、OpenLoader、KubeJS 等支援的文字來源：依工具支援範圍輸出。

工具不會翻譯圖片裡的字，也不會修改 `mods/*.jar`。

### 按鈕說明

| 按鈕 | 用途 |
|---|---|
| 開始翻譯 | 第一次建立完整翻譯結果。 |
| 補翻工作階段 | 上次中斷、或新增文字後補做未完成部分。 |
| 套用到遊戲 | 複製翻譯結果到整合包，會先備份可能被覆蓋的檔案。請先關閉 Minecraft。 |
| 修復工作階段 | 找不到工作階段或輸出不完整時重新整理工作階段資訊。 |
| 術語表 | 固定模組專有名詞的翻譯，例如物品、怪物或機器名稱。 |

## 字體資源包服務

字體資源包是獨立服務，不會翻譯文字。它會把使用者選取的字體檔和 Minecraft 字體設定打包成資源包，讓遊戲文字與模組介面使用喜歡的字體。

### 建立方式

1. 切到「字體資源包」頁面。
2. 選取你有權使用的 `.ttf`、`.otf` 或 `.ttc` 字體檔。
3. 選取字體包輸出位置並設定資源包名稱。
4. 調整設定後按「建立字體資源包」。
5. 把輸出的資源包放進啟動器實例的 `resourcepacks`，再到 Minecraft 的「設定 → 資源包」啟用。

### 進階設定

| 設定 | 說明 |
|---|---|
| 字體大小 | 調整 Minecraft 字體 provider 使用的基準尺寸。 |
| 字重感 | 在 renderer 層做小幅近似調整。它不會改寫字體檔的真正筆畫。 |
| 水平位移 | 修正字體在遊戲中偏左或偏右。 |
| 垂直位移 | 修正字體在遊戲中偏上或偏下。 |
| 清晰度 | Minecraft 的 `oversample` 設定。越高通常越平滑，也可能增加資源需求。 |

如果需要真正的粗體或細體，請直接選取對應字重的字體檔，例如 Regular、Medium、SemiBold 或 Bold。工具不會重新繪製或修改 TTF/OTF/TTC 原始檔。

建立後會同時寫入 `default.json` 與 `uniform.json`，讓一般文字和 Unicode 文字都能使用。

## 輸出資料夾

輸出根目錄通常會包含：

```text
翻譯結果/
├─ resourcepacks/
├─ config/
├─ patchouli_books/
├─ 翻譯工作階段.json
├─ 覆蓋範圍說明.txt
└─ 翻譯錯誤日誌.txt
```

實際內容會依整合包使用的模組和文字來源而不同。

## AI、金鑰與隱私

- AI 是選用功能。關閉 AI 仍可做簡繁轉換、掃描、分類和資源包輸出。
- API 金鑰只儲存在本機設定檔，不會寫進翻譯結果或程式預設值。
- 送往 AI 的內容只包含待翻譯字串。請自行確認你使用的 AI 服務條款。
- 不要把含有個人資料、私密伺服器資訊或不應外傳內容的字串送往第三方服務。
- AI 可能產生錯誤、過於直譯或缺少上下文的結果。遇到重要名稱請使用術語表修正。

## 常見問題

### 遊戲出現紅字或閃退

先停用輸出的資源包，確認原本整合包可以正常啟動。語言檔工具不會修復缺少模組、版本不相容、世界存檔或其他模組本身的問題。

### 中文變成方框

切換到「字體資源包」，選擇支援繁中的字體檔並建立資源包，再到遊戲資源包選單啟用。也可以關閉「強制使用 Unicode 字型」後重新啟動遊戲。

### 翻譯結果沒有全部完成

翻譯覆蓋範圍會受到整合包版本、文字來源、AI 設定和工具支援範圍影響。請查看「覆蓋範圍說明.txt」和「翻譯錯誤日誌.txt」，必要時使用「補翻工作階段」。

### 圖片裡的文字會翻嗎？

不會。目前只處理遊戲文字與相關語言檔，不處理圖片 OCR。

## 免責說明

本工具由 ZeitFrei 以方便玩家整理模組包文字為目的提供。翻譯結果可能因模組版本、原文上下文、第三方 AI 服務或字體檔品質而有錯誤、不完整或不符合個人偏好的情況。

使用者應自行確認模組整合包、字體檔、翻譯內容及第三方服務的授權與使用條款。請不要使用你沒有權利散布的字體或內容。

本工具不保證遊戲一定能啟動、不保證翻譯完整、不負責模組本身的錯誤、存檔損壞、版本衝突、第三方 API 費用或任何因使用輸出結果造成的直接或間接損失。套用前請備份整合包與存檔，並在 Minecraft 關閉時操作。

最後更新：2026-08-10（台北）
